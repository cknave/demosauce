libReplayGain is a wrapper around mp3gain's analyze module, slightly modified to 
handle multiple instances. throw in a few conversion funfctions and there you have it. 
license is lgpl, i guess.

based on mp3gain 1.5.1 
